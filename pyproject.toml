[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tinydata"
version = "1.2.3"
description = "A lightweight Tinysoft TS-OPI data client with pandas-friendly dataset APIs."
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name = "wuh" }
]
dependencies = [
  "pandas>=2.2",
  "pyarrow>=15",
  "platformdirs>=4",
  "pydantic>=2",
  "python-dateutil>=2.8",
  "tqdm>=4.66",
]

[project.optional-dependencies]
test = [
  "pytest>=7",
  "pytest-asyncio>=0.21",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["tinydata", "tinydata.*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = ["--strict-markers", "--tb=short"]
markers = [
  "requires_tinysoft: tests that require a real Tinysoft TS-OPI connection",
]
